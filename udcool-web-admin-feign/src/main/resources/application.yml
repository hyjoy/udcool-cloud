spring:
  application:
    name: udcool-web-admin-feign
  thymeleaf:
    cache: false
    mode: LEGACYHTML5
    encoding: UTF-8
    servlet:
      content-type: text/html
server:
  port: 9005

eureka:
  client:
    service-url:
      defaultZone: http://localhost:9001/eureka/
hystrix:
  metrics:
    enabled: true

